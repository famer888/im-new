---
description: im-new 开发必须对齐 im，并避免无关改动
globs:
  - "src/**/*"
  - "src-tauri/**/*"
  - "package.json"
  - "vite.config.ts"
alwaysApply: true
---

- `im` 是旧项目，也是产品行为基准；`im-new` 是重构目标。
- `im-new` 主要交付 mac 端和 Windows 端，同时保持网页端可用；不要只按浏览器网页端设计或验证，也不要默认打开浏览器端验证。
- “90%参考 `im`”表示功能、交互、流程、数据含义、协议含义、事件时机和最终结果要尽量对齐 `im`；实现方式可以按 `im-new` 的 Vue3 + Tauri 架构重写。
- 默认只改 `im-new`。不要修改 `../im`，除非用户明确要求。
- 改 `im-new` 前，必须先找 `../im` 中对应的旧实现，并保留旧项目行为。
- 使用最小安全改动。不要做无关重构、重命名、批量格式化、依赖升级或目录搬迁。
- 修改哪里就只修改哪里。发现其它错误、隐患或可优化点时，先提醒用户；不要顺手扩大范围去修，除非它直接阻塞当前任务。
- 新增或修改业务逻辑、平台逻辑、兼容旧项目逻辑、兜底逻辑、错误处理、协议/数据转换、复杂判断、异步流程、状态更新时，必须写简短注释说明这段代码做什么、为什么这样做；只改样式、文案、简单赋值或普通调用时可以不写。
- 涉及窗口、托盘、通知、文件、截图、剪贴板、下载、路径、权限、快捷键、安装包或系统能力时，优先考虑 mac 和 Windows，同时保留网页端兜底。
- 验证界面、窗口、文件、通知、剪贴板、下载、登录、消息等桌面端功能时，优先运行或打开 Tauri 桌面应用；浏览器端只用于明确的网页兼容检查或无法验证桌面端时的补充。
- 不要随意改变路由、桥接约定、Tauri 命令名、存储 key、会话 ID、缓存 key、事件名、协议含义、数据库含义或 WebSocket 重连策略。
- 默认保护这些产品语义：文件助手兼容、`invitation`、`channelNotice`、未读数、草稿、通知、撤回、删除、定时删除、域名兜底、下载、解密、打开文件、托盘未读数。
- 高风险区域：`src/proto/*`、`src/api/*`、`src/stores/*`、`src/plugins/tauri-events.ts`、`src/shims/tauri-electron-bridge.ts`、`src-tauri/src/db/*`、`src-tauri/src/ws/*`、`src-tauri/src/commands/*`、`src-tauri/src/crypto/*`。
- 构建、打包、图标、权限、发布脚本默认不要碰，除非任务明确要求。
- 开始前先定义完成标准。汇报前要尽可能验证；只改规则或文档时，要读回改过的文件并检查结构。
- 向用户汇报时，用简单直白的话说明做了什么、结果怎样、还有没有剩下的问题。最终回复避免不必要的技术细节。
- 有疑问时，先对比 `../im`，优先选择兼容旧项目行为。
