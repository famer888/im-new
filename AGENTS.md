# im-new 项目规则

## 目标

`im` 是旧项目，也是产品行为基准。
`im-new` 是重构目标。

`im-new` 的主要交付目标是 mac 端和 Windows 端，同时保持网页端可用。不要只按浏览器网页端来判断方案，也不要默认打开浏览器端做验证。

“`im-new` 参考 `im` 90%左右”的意思是：用户看到的功能、交互、流程、数据含义、协议含义、事件时机和最终结果，都要尽量和 `im` 对齐；具体实现可以按 `im-new` 当前的新架构重写。

默认优先级：

1. 先对齐 `im` 的行为。
2. 再遵守 `im-new` 现有结构。
3. 优先保证 mac 端和 Windows 端可用，同时保持网页端兼容。
4. 最后才在本次任务范围内改善实现质量。

如果某个改动不是本次任务必须的，就不要碰。

## 旧项目参考位置

行为不清楚时，先看这些 `im` 文件：

- 应用入口和路由：`../im/src/main.js`、`../im/src/routers.js`
- 登录页和主界面：`../im/src/pages/login/index.vue`、`../im/src/pages/home/index.vue`
- Electron 主进程和通知：`../im/src/background.js`、`../im/src/notification/index.js`
- 平台桥接：`../im/src/platform.js`
- WebSocket 生命周期和重连：`../im/src/socket/index.js`、`../im/src/socket/analyst.js`
- WebSocket 消息分发：`../im/src/event/index.js`
- 消息、会话列表、群组、频道、好友事件：`../im/src/event/msg.js`、`../im/src/event/chat.js`、`../im/src/event/group.js`、`../im/src/event/channel.js`、`../im/src/event/friend.js`
- 文件下载、解密、打开流程：`../im/src/event/file.js`、`../im/src/utils/fileTools.js`、`../im/src/utils/upload.js`
- 接口和协议处理：`../im/src/api/imBase.js`、`../im/src/api/base/*`
- 本地数据库和缓存：`../im/src/database/*`、`../im/src/cache/*`
- 阅后即焚和定时删除：`../im/src/event/cheduled-deletion.js`

在 `im-new` 中对应查看这些位置：

- 应用入口和路由：`src/main.ts`、`src/router/index.ts`
- 登录页和主界面：`src/modules/auth/views/*`、`src/layouts/MainLayout.vue`
- Tauri 桥接：`src/shims/tauri-electron-bridge.ts`
- 前端事件监听：`src/plugins/tauri-events.ts`
- Tauri 后端入口和命令：`src-tauri/src/lib.rs`、`src-tauri/src/commands/*`、`src-tauri/src/window/*`
- WebSocket：`src-tauri/src/ws/*`、`src/utils/networkDiagnostics.ts`
- 接口、协议、域名池：`src/api/*`、`src/proto/*`、`src/utils/domainPool.ts`
- 状态和产品语义：`src/stores/*`
- 本地数据库：`src-tauri/src/db/*`

## 必须遵守的规则

1. 默认只改 `im-new`。
   不要修改 `../im`，除非用户明确要求也改旧项目。

2. 改 `im-new` 前，先对照 `im`。
   要保留旧项目里的路由行为、事件时机、未读规则、草稿规则、通知规则、重连规则、文件处理、本地数据表现和可见交互。

3. 使用最小安全改动。
   不做无关清理、重命名、批量格式化、目录搬迁、依赖升级或架构调整，除非任务明确要求。

4. 修改哪里就只修改哪里。
   发现其它位置有错误、隐患或可优化点时，先在汇报里提醒用户，不要顺手扩大范围去修。只有这个问题直接阻塞当前任务时，才做完成当前任务所需的最小处理，并说明原因。

5. 不要绕开 `im-new` 现有边界。
   继续遵守当前的分层：界面、状态、接口/桥接、Tauri 命令、WebSocket、本地数据库。

6. 写代码必须补关键注释。
   新增或修改业务逻辑、平台逻辑、兼容旧项目逻辑、兜底逻辑、错误处理、协议/数据转换、复杂判断、异步流程、状态更新时，必须在代码块上方或关键分支旁写简短注释，说明这段代码做什么、为什么这样做。只改样式、文案、简单赋值或普通调用时可以不写，但交付前必须主动检查有没有漏掉该写的注释。

7. mac 端和 Windows 端是主要目标。
   涉及窗口、托盘、通知、文件、截图、剪贴板、下载、路径、权限、快捷键、安装包或系统能力时，必须考虑 mac 与 Windows 的差异，同时保留网页端可用的兜底方式。不要只按网页端能力设计或验证。

8. 验证时优先使用桌面端。
   这个项目主要开发桌面端，涉及界面、窗口、文件、通知、剪贴板、下载、登录、消息等功能时，不要默认打开浏览器端验证；优先运行或打开 Tauri 桌面应用。浏览器端只在任务明确涉及网页兼容，或当前无法验证桌面端时作为补充，并在汇报里说明。

9. 不要随意改有兼容风险的标识。
   包括会话 ID、伪会话 ID、存储 key、命令名、事件名、协议字段含义、数据库字段含义、窗口标识和缓存 key。

10. 旧项目里的产品语义默认受保护。
   不要随意改变：`FILE_HELPER_TARGET_ID`、文件助手兼容逻辑、群通知伪会话、频道通知伪会话、未读数、草稿、撤回、删除、阅后即焚、定时删除、域名兜底、WebSocket 心跳和重连、通知、托盘未读数、文件下载、解密和打开逻辑。

11. 协议、加密、本地数据是高风险区域。
   没有明确任务和对照验证时，不要改这些位置：
   `src/proto/*`、`src/api/request.ts`、`src/utils/e2ee.ts`、`src-tauri/src/crypto/*`、`src-tauri/src/db/*`、`src-tauri/src/ws/*`。

12. 构建和发布相关内容默认不要碰。
   除非任务就是构建、发布、图标、签名或应用外壳，否则不要改：
   `package.json`、`vite.config.ts`、`src-tauri/tauri.conf.json`、`src-tauri/capabilities/*`、`src-tauri/icons/*`、`resources/*`、发布脚本。

13. 界面改动也要对齐旧项目行为。
   只有用户明确要求视觉升级时，才允许主动做视觉现代化。否则保持旧项目的流程、信息密度、控件位置、文案含义和消息表现。

14. 如果 `im` 没有完全对应的实现，不要自己发明大逻辑。
    选择最接近的旧逻辑参考，改动保持局部，并在最终汇报里说明这个判断。

## 工作方式

做 `im-new` 的非简单任务时，按这个顺序来：

1. 先定义完成标准。
2. 找到 `im-new` 里要改的位置。
3. 找到 `im` 里对应的旧实现。
4. 确认哪些行为必须保持一致。
5. 只改 `im-new` 中必要的文件。
6. 检查没有影响无关路由、命令、状态、本地数据和用户可见行为。
7. 如果发现与当前任务无关的问题，只提醒用户；不要把它混进本次改动。
8. 检查新增或修改的代码块是否已有必要注释；如果没有注释，必须确认它只是样式、文案、简单赋值或普通调用。

## 完成和验证

不要把“写完文件”当成完成。

- 能运行就运行。
- 有相关检查或测试，就跑相关检查或测试。
- 做界面或可视化内容时，要优先打开桌面端应用窗口检查，并走一遍关键交互；不要把浏览器页面检查当成桌面端验证。
- 涉及桌面端能力时，要优先考虑 mac 和 Windows；如果当前环境只能验证其中一端或只能验证网页端，汇报时要直接说明。
- 做脚本时，要用真实或有代表性的输入跑一遍，并检查输出。
- 能安全模拟边界情况时，要试一下。
- 验证失败就先修，再重新验证。
- 只有确认可用，或确实需要用户介入时，才回来汇报。

默认检查方式：

- 只改规则或文档：读回改过的文件，检查内容完整、结构正常。
- 改前端或运行逻辑：条件允许时至少跑 `pnpm type-check`。
- 改构建、打包或 Tauri 边界：条件允许时跑对应的构建或检查命令。
- 改窗口、文件、通知、剪贴板、下载、路径、安装包等桌面能力：条件允许时分别检查 mac 和 Windows 影响；不能实测的端要说明。

## 默认不要改的内容

除非用户明确要求，不要改：

- `src/router/index.ts` 的路由结构
- `src/main.ts` 的启动顺序
- `src/shims/tauri-electron-bridge.ts` 的桥接约定
- `src-tauri/src/lib.rs` 的命令注册结构
- `src-tauri/src/db/migrations.rs` 的数据结构
- `src-tauri/src/ws/*` 的重连和诊断流程
- `src/proto/generated.js` 和生成类型
- 包管理器、依赖版本、发布脚本、品牌资源、图标

## 向用户汇报

向用户汇报时：

- 用简单直白的话说。
- 说明做了什么、结果怎样、还有没有剩下的问题。
- 最终回复不要堆术语、实现细节和代码腔。
- 如果必须提文件或命令，只提理解结果所需的少量内容。
- 不要把过程写成流水账。
- 如果发现了当前任务之外的问题，要单独提醒，说明是否影响本次结果。
- 如果没有偏离 `im`，就说明结果保持了和 `im` 的对齐。
- 如果有内容没法验证，要直接说明原因。

实际干活时可以严谨、技术化；但给用户看的汇报要像给一个聪明但没看代码的人解释。

## 判断倾向

有疑问时：

- 先对比 `im`
- 选择兼容，而不是炫技
- 选择小改，而不是大改
- 选择不乱改，而不是猜测式改动
- 先验证，再汇报
